# AI_TestTask

# Пайплайн для аналізу служби підтримки за допомогою ШІ 🤖

Повністю автоматизований пайплайн, створений для генерації та аналізу діалогів служби підтримки клієнтів з використанням локальних великих мовних моделей (LLM) через Ollama та LangGraph. Він також включає міні Telegram-бота для зручної взаємодії.

## 📑 Основні можливості
Відповідно до вимог тестового завдання, це рішення складається з двох основних компонентів:

1. **Генерація датасету (`generate.py`)**:
   * Генерує різноманітні, багатоетапні діалоги між клієнтом та сапорт-агентом.
   * Охоплює конкретні сценарії: проблеми з оплатою, технічні помилки, доступ до акаунту, питання по тарифу та повернення коштів.
   * Симулює різні типи кейсів: успішні, проблемні, конфліктні та випадки з помилками агента.
   * Результат зберігається у структурованому вигляді у файл `data/raw_dialogues.json`.

2. **Аналіз діалогів (`analyze.py`)**:
   * Опрацьовує згенерований датасет та оцінює взаємодію.
   * Визначає **intent** (тему) звернення.
   * Оцінює реальну **satisfaction** (задоволеність) клієнта (satisfied / neutral / unsatisfied), цілеспрямовано шукаючи приховану незадоволеність.
   * Присвоює **quality_score** (оцінку якості від 1 до 5) роботі агента.
   * Ідентифікує конкретні помилки агента **agent_mistakes** (наприклад: проігноровані питання, некоректна інформація, грубий тон, погана граматика, використання російських слів/суржику).
   * Результат аналізу зберігається у файл `data/analyzed_dialogues.json`.

* Обидва файли використовують LangGraph, що у майбутньому дозволить швидке додавання нових модулів, чи змінення існуючих

## 🛠 Вимоги до системи

Перед запуском проєкту переконайтеся, що у вас встановлено:
* **Python 3.10+**
* **Ollama:** На вашому комп'ютері має бути встановлена та запущена [Ollama](https://ollama.com/).

Перед запуском скриптів завантажте необхідні моделі через термінал *(Примітка: у коді використовуються моделі `gemma3:12b` та `llama3.1`, ви можете змінити на більш потужні за потреби)*:


```bash
ollama run llama3.1
ollama run gemma3:12b
```


## Встановлення та налаштування
Склонуйте репозиторій:


```bash
git clone https://github.com/Mafusail2020/AI_TestTask.git
cd AI_TestTask
```

Створіть та активуйте віртуальне середовище:

```bash
python -m venv .venv
source .venv/bin/activate  # Для Windows використовуйте: .venv\Scripts\activate
```

Встановіть залежності:

```bash
pip install -r requirements.txt
```

### За бажанням
Налаштуйте змінні середовища:
Створіть файл .env у кореневій папці проєкту та додайте ваш токен Telegram-бота (його можна отримати у @BotFather):

```python
TOKEN=ваш_токен_телеграм_бота_тут
```

## Використання
Ви можете запускати пайплайн вручну через командний рядок або інтерактивно за допомогою Telegram-бота.

Варіант А: Telegram Бот (Рекомендовано та просто)
Запустіть Telegram-бота для зручного інтерфейсу користувача:

```bash
python bot.py
```

Відправте команду /start боту.

Натисніть Згенерувати датасет, щоб LLM згенерувала діалоги.

Натисніть Аналізувати датасет, щоб обробити згенерований файл і отримати результати аналізу безпосередньо в чат.

Варіант Б: Через термінал
Запустіть скрипт генерації напряму:

```bash
python generate.py
```
Після його завершення, запустіть скрипт аналізу:

```bash
python analyze.py
```

```
### 📁 Структура проєкту
├── .env                        # Змінні середовища (Токен бота)
├── requirements.txt            # Залежності Python
├── bot.py                      # Точка входу для Telegram-бота
├── config.py                   # Конфігурація та завантаження .env
├── generate.py                 # LangGraph пайплайн для генерації датасету
├── analyze.py                  # LangGraph пайплайн для QA аналізу діалогів
├── graph_utils/
│   └── states.py               # Pydantic схеми станів (states) для LangGraph
└── data/                       # Папка для вхідних/вихідних файлів
    ├── raw_dialogues.json      # Згенерований датасет
    └── analyzed_dialogues.json # Оцінені діалоги після аналізу```
